# 今後にやること、やりたいことのメモ

## 言葉の定義

-- 路線 : 電車などの交通機関(灰色チューブ)
-- 経路 : 駅出口からクリサムを結ぶ歩行経路(オレンジ色チューブ)

## 歩行時のアニメーション

-- 開始点から進むとき、3秒間のカウントダウン → OK
-- 目標に着いたら停止し、目標を向き、少し見上げて3秒間止まる → NG。目標(クリサムの建物、逆順の場合は駅出口の重心点)を向いていない → OK
-- 終了点から開始点へ移動するとき、フェードアウト->開始点へ移動->フェードイン → OK
-- PiPでも同じ → OK

## 歩行時は路線は表示しない

-- PiPでも同じ → OK → NGに訂正。歩行時に路線が表示される(歩行時のメイン側、俯瞰時のPiP歩行側)。 → OK

## ** 追加 ** PiP内の表示

-- 俯瞰時に経路を表示 → OK

## 操作パネル

### 再生部分

-- 段組として一段別にする → OK
-- 速度スライダー部分は廃止 → OK
-- 音楽プレーヤーのように　再生、停止、一次停止、早送り、早戻り、スライダー　とする → OK
-- スライダーは速度ではなく、移動量(メートル) → OK

### カメラ操作

-- １行で表す → OK

## レイアウト

-- 操作パネルとPiPの高さを揃える → OK
